# pytest configuration
[pytest]
python_files = test_*
python_classes = *Tests
python_functions = test_*

# Custom markers
filterwarnings = ignore:.*PytestUnknownMarkWarning*
markers =
    images: all tests within image_downloader module
    tweepy: all tests within tweepy_image_tweeter module
    focus: temp, used for focusing on a single problem func